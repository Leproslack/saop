<div class="form-group">
  <div class="col-sm-4">
    <legend><%= f.object[:category_name] %></legend>
      <%= f.hidden_field :category_name %>
      <%= f.hidden_field :category_score%>
  </div>
</div>
<div class="form-group">
    <%= f.fields_for :questions do |builder| %>
      <%= render 'question_fields', :f => builder %>
    <% end %>
</div>
