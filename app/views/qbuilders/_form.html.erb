<%= form_for(qbuilder) do |f| %>
  <% if qbuilder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qbuilder.errors.count, "error") %> prohibited this qbuilder from being saved:</h2>

      <ul>
      <% qbuilder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
