<%= form_for(store) do |f| %>
  <% if store.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% store.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Название магазина" %>
    <%= f.text_field :store_name %>
  </div>

  <div class="field">
    <%= f.label "Адрес магазина" %>
    <%= f.text_field :store_adress %>
  </div>

  <div class="field">
    <%= f.label "Категория магазина" %>
    <%= f.text_field :store_caregory %>
  </div>

  <div class="actions">
  <%= f.submit "Создать магазин"%>
  </div>
<% end %>
