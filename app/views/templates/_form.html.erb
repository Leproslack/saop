<div class="rows">
<%= nested_form_for(template, html: { class: "form-horizontal" }) do |f| %>
  <% if template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(template.errors.count, "error") %> prohibited this template from being saved:</h2>
      <ul>
      <% template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <div class="col-sm-2">
      <%= f.label :category %>
      <%= f.text_field :category, class: "form-control" %> <br />
    </div>
  </div>
          <%= f.fields_for :template_fields do |builder| %>
          <%= render "form_template_fields", :f => builder %>
          <% end %>
          <%= f.link_to_add "Add a task", :template_fields %>
          <%= f.submit "Save", class: "btn btn-info" %>
<% end %>
</div>
